# Package source

Installation and usage are documented in the [repository README](../README.md).
The installable project metadata lives at the repository root. This directory
contains the Python source and tests; it is not a separate installation project.
